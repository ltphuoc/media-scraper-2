config:
  target: 'http://localhost:4000'
  phases:
    - duration: 5
      arrivalRate: 5
  http:
    timeout: 10
  defaults:
    headers:
      Content-Type: 'application/json'
      Authorization: 'Basic YWRtaW46YWRtaW4='

scenarios:
  - name: 'Smoke test scrape endpoint'
    flow:
      - post:
          url: '/scrape'
          json:
            urls:
              - 'https://picsum.photos'
          expect:
            - statusCode: 202
