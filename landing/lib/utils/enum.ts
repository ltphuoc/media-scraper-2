export enum SEARCH_PARAMS {
  Page = 'page',
  Limit = 'limit',
  Type = 'type',
  Search = 'search',
  Mode = 'mode',
  View = 'view',
}

export enum MODE {
  Pagination = 'pagination',
  Infinite = 'infinite',
}

export enum VIEW {
  Grid = 'grid',
  List = 'list',
}
